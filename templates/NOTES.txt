Thank you for installing {{ .Chart.Name }}!

Your JupyterHub deployment is now being set up.

To access JupyterHub:

1. Get the application URL by running these commands:
{{- if contains "NodePort" .Values.jupyterhub.proxy.service.type }}
  export NODE_PORT=$(kubectl get --namespace {{ .Release.Namespace }} -o jsonpath="{.spec.ports[0].nodePort}" services {{ .Release.Name }}-proxy-public)
  export NODE_IP=$(kubectl get nodes --namespace {{ .Release.Namespace }} -o jsonpath="{.items[0].status.addresses[0].address}")
  echo http://$NODE_IP:$NODE_PORT
{{- else if contains "LoadBalancer" .Values.jupyterhub.proxy.service.type }}
  NOTE: It may take a few minutes for the LoadBalancer IP to be available.
        You can watch the status by running:
        kubectl get --namespace {{ .Release.Namespace }} svc -w {{ .Release.Name }}-proxy-public

  export SERVICE_IP=$(kubectl get svc --namespace {{ .Release.Namespace }} {{ .Release.Name }}-proxy-public --template "{{"{{ range (index .status.loadBalancer.ingress 0) }}{{.}}{{ end }}"}}")
  echo http://$SERVICE_IP
{{- else if contains "ClusterIP" .Values.jupyterhub.proxy.service.type }}
  export POD_NAME=$(kubectl get pods --namespace {{ .Release.Namespace }} -l "app=jupyterhub,component=proxy" -o jsonpath="{.items[0].metadata.name}")
  kubectl --namespace {{ .Release.Namespace }} port-forward $POD_NAME 8080:8000
  echo "Visit http://127.0.0.1:8080 to access JupyterHub"
{{- end }}

2. Default authentication is set to 'dummy' - any username/password works.
   WARNING: Change this before deploying to production!

For more information, visit: https://github.com/nebari-dev/nebari-data-science-pack
